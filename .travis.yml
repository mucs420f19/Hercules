dist: bionic
sudo: required
language:
  - cpp
python: "3.7"
os:
  - linux
  - osx
  - windows
compiler:
  - gcc
  - clang
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-6
      - g++-6
      - cmake
install:
  - pip install conan --user
  - conan user
script:
  - mkdir build
  - cd build
  - conan install .
  - cmake ..
  - cmake --build .
  - ctest
notifications:
  email: false